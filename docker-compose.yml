version: '3'
services:
  kite-api:
    build: ./kite-api
    volumes:
       - "C:/public-html/:/app/public/"
    networks:
        - kite
  kite-nginx:
    build: ./kite-nginx
    ports:
     - "8080:80"
    networks:
        - kite
networks:
  kite:
    driver: bridge